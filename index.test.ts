import {expect, test, describe} from 'bun:test';

import Server from './index'
describe('Render the home page',()=>{
    test('See the home text',()=>{
// Server();
// expect(getByText("Home Page!")).toBeInTheDocument();
expect(2+2).toBe(4);
    })
})